apiVersion: v1
kind: Service
metadata:
  name: quasar-frontend
spec:
  selector:
    app: quasar-frontend
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
  type: NodePort
