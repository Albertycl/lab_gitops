apiVersion: v1
kind: Service
metadata:
  name: pure-flask
  labels:
    app: pure-flask
spec:
  selector:
    app: pure-flask
  ports:
    - name: http
      port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
